import { RouteStub } from "@/components/layout/route-stub";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <RouteStub
      title="Contact us"
      description="Helpline, email, WhatsApp, and enquiry form."
      phase="Phase 9"
    />
  );
}
